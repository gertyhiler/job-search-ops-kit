# Prompt additions

Candidate-specific instructions for the chat agent and AI subprocesses.
Repo prompts (`prompts/*.prompt.md`) and `AGENTS.md` stay generic; fill this file during `/init`.

Sections `## all` and `## <prompt-name>` are merged into subprocess prompts (e.g. `cover-letter`, `questionnaire`).
Section `## agent` is for the chat agent only (`read_profile` → follow it when drafting manually).

## all

<!-- TODO: default language, tone, contacts policy, facts vs stack tenure -->

## agent

<!-- TODO: submit confirmation phrases, browser policy, vacancy-specific bans -->

## cover-letter

<!-- TODO: opening/closing style, word limit overrides, contact block -->

## questionnaire

<!-- TODO: answer structure, topics to avoid in letters vs questionnaires -->
